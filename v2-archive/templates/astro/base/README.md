# Astro Base Templates

The first v2 generator writes its Astro templates from Python so the vertical
slice is self-contained. Once the shape stabilizes, move those templates here
and let the generator copy/render them.
