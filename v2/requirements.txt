# v2-specifieke dependencies, los van root requirements.txt zodat v1 niet
# onnodig herbouwd hoeft te worden.
#
# Installatie in de site-factory-worker container:
#   docker exec site-factory-worker pip install -r /workspace/v2/requirements.txt
#
# Of permanent door deze regels mee te nemen in een toekomstige worker-image
# rebuild.

colorthief>=0.2.1
materialyoucolor>=2.0.10
Pillow>=10.0.0
